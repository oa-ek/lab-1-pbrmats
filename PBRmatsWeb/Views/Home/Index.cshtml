@{
    ViewData["Title"] = "Materials";
}

<div class="p-0 m-0 d-flex flex-column h-100">
    <div class="m-0 p-0 pt-2 d-flex flex-row gap-2 pa-2 flex-grow-1 overflow-hidden">
        <div class="flex-shrink-0 flex-grow-0 d-flex flex-column">
            Test
        </div>
    </div>
</div>